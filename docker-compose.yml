services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: v4lentinenam/javascript-fastify-blog:latest
    working_dir: /app
    environment:
      - NODE_ENV=test
    command: ["make", "test"]

