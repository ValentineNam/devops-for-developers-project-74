version: '3.9'

services:
  app:
    build: .
    container_name: fastify-blog
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=development
    volumes:
      - ./app:/app
    stdin_open: true
    tty: true
